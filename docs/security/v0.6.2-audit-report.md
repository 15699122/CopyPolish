# v0.6.2 安全维护审计报告（S2-G）

> 审计日期：2026-09-07  
> 审计范围：v0.6.2 纯隐私、安全、依赖与供应链维护  
> 审计状态：**有条件通过，尚未达到正式发布批准条件**

## 1. 结论摘要

本次审计确认 v0.6.2 维护变更已经覆盖隐私扫描、IPC 错误边界、设置存储安全、GitHub Actions 权限与 SHA 固定、SBOM/license/checksum 校验，以及 TUI 依赖升级。PR #54 的 GitHub Actions CI 已于 **2026-09-07** 全部通过。

但当前不能批准正式发布，原因是：

1. PR #54 已于 **2026-09-07 03:00:58 UTC** squash 合并到 `dev`，合并提交为 `9dc2e0765b6826b50c66b2bc8fe2039ca2f2a0e9`；合并后的 `dev` CI run `34078187687` 已全部通过，但 Security API 告警状态仍需继续复核；
2. GitHub Security API 在 **2026-09-07** 仍报告 3 个 Dependabot 告警和 4 个 CodeQL 告警；当前 CodeQL API 返回的告警 ref 仍为 `refs/heads/master`，尚未取得覆盖 `dev` 合并提交的新分析结果；
3. E2E 的 `extract-zip` high 告警仍是已登记的测试工具链风险接受项，复核日期为 **2026-10-06**；
4. `glib 0.18.5` medium/unsound 告警仍按独立风险接受处理，复核日期为 **2026-12-31**；
5. Windows Terminal 原生 TUI 交互回归、Windows 设置安全 artifact 证据和正式预发布演练尚未完成；
6. 按项目规则，尚未获得正式发布批准，因此不创建 `v0.6.2` tag、不构建、不发布。

## 2. 已验证项目

### 2.1 CI 与本地门禁

PR #54（`deps/tui-stack-0.30`，HEAD `e43ad44d93d46c05c6e3c255761393b165207428`）对应 GitHub Actions run `34077703751`，于 **2026-09-07** 完成；随后合并到 `dev` 的 commit `9dc2e0765b6826b50c66b2bc8fe2039ca2f2a0e9` 对应 CI run `34078187687`，三个 job 也全部通过：

| Job | 结果 |
| --- | --- |
| Rust (fmt / clippy / test) | 通过 |
| Frontend (vitest / build) | 通过 |
| Security and docs | 通过 |

合并后的 `dev` CI 结果：Rust、Frontend、Security and docs 均通过。

工作树在提交审计时保持干净。以下本地检查通过：

- `python3 scripts/verify.py --profile audit`
- `python3 scripts/generate_licenses.py --check`
- `python3 scripts/check_md_links.py`
- `git diff --check`
- `cargo metadata --manifest-path src-tauri/Cargo.toml --locked`
- `cargo metadata --manifest-path src-tauri/Cargo.toml --locked --features tui`
- `python3 scripts/security_check.py --require-sops`

### 2.2 依赖与供应链

- `ratatui` 升级至 `0.30.2`；
- `crossterm` 升级至 `0.29.0`；
- `lru` 锁定至 `0.18.4`，覆盖 `GHSA-rhfx-m35p-ff5j` 修复版本；
- `docs/licenses.md` 已依据当前 Cargo/npm lockfile 重新生成并通过一致性检查；
- S2-F 的 SBOM、checksum、license 和 Release 资产校验已接入统一审计门禁。

### 2.3 隐私与安全边界

- 用户正文默认不落盘，`restore_last_input` 关闭时保存前归一化为空；
- 设置写入包含原子替换、备份恢复、Unix `0600`、symlink 拒绝和进程内串行化；
- IPC 使用稳定 `{ code, message }` 错误协议，前端不展示原始内部错误；
- 凭据扫描不会把命中源行或凭据原文写入诊断输出；
- CI job 使用最小 `contents: read` 权限，第三方 Actions 使用固定 commit SHA；
- Release 校验包含动态 notes、`expected_sha`、SBOM、checksum 和许可证清单终验。

## 3. 当前告警与决定

### 3.1 Dependabot

截至 **2026-09-07**，默认分支 Security API 返回：

| 告警 | 严重性 | 当前决定 |
| --- | --- | --- |
| `extract-zip` / `GHSA-jmr9-qjv8-65gv` | high | 仅 E2E 测试工具链；已有正式限期风险接受，复核日 2026-10-06 |
| `lru` / `GHSA-rhfx-m35p-ff5j` | low | PR #54 已合并，`dev` 已锁定 `lru 0.18.4`；Security API 仍显示 open，待 GitHub Dependabot 重新评估后确认关闭或记录延迟 |
| `glib 0.18.5` / `GHSA-wrw7-89jp-8q8g` | medium | Tauri/Wry GTK 代际迁移前限期接受，复核日 2026-12-31 |

`extract-zip` 不进入生产 Release；`lru` 属于 PR #54 的可修复传递依赖；`glib` 的风险接受记录见 [glib-0.18-soundness-risk.md](../decisions/glib-0.18-soundness-risk.md)。

### 3.2 CodeQL

截至 **2026-09-07**，CodeQL API 返回的告警仍指向 `refs/heads/master`：

- `py/clear-text-logging-sensitive-data`：旧告警，代码已重构并加入回归测试；
- 3 个 `actions/missing-workflow-permissions`：workflow 已显式声明 `contents: read`。

由于最新 CodeQL 分析尚未覆盖 `dev` 合并提交，在 GitHub Security API 完成对应重新扫描前，不把这些项目标记为已关闭。

## 4. 未完成项与发布阻断项

| 项目 | 状态 | 后续动作 |
| --- | --- | --- |
| PR #54 合并到 `dev` | 已完成 | 合并提交 `9dc2e0765b6826b50c66b2bc8fe2039ca2f2a0e9`；合并后 CI 全部通过 |
| 合并后 Dependabot/CodeQL 复核 | 待完成 | 重新读取告警状态并保存证据 |
| Windows Terminal TUI 交互回归 | 待完成 | 发布演练按 Runbook 执行 |
| Windows 设置安全 artifact 证据 | 待完成 | 保留脱敏、最小化、不可逆泄露的验证记录 |
| `master` 发布基线 | 待完成 | 仅在 `dev` 验证完成后推进 |
| `publish=false` 预发布演练 | 待完成 | 仅在维护者明确授权后执行 |
| v0.6.2 正式发布 | 阻断 | 当前不得创建 tag、构建或发布 |

## 5. 审计决定

S2-G 本轮结论为：**有条件通过依赖与代码维护审计，但发布门槛未全部满足**。PR #54 已合并到 `dev`，且合并后 CI 全部通过；合并不等于 v0.6.2 发布批准。正式发布前必须完成第 4 节中的阻断项，并由维护者明确批准版本提升、`master` 基线、预发布演练和正式发布。
