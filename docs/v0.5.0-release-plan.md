# CopyPolish `v0.5.0` 正式发布开发计划

本文档是 CopyPolish（文案净排）`v0.5.0` 正式发布前的开发与验收计划。计划基于当前 `dev` 分支、Tauri 2 + React + Rust 架构以及现有 CI / Release 流程制定。

## 1. 发布目标与范围

### 本轮目标

发布 CopyPolish / 文案净排 `v0.5.0` 正式版。

### 本轮优先级

1. 先保证格式化正确性，再扩展功能；
2. 先建立测试基座，再新增规则或大型 UI 功能；
3. 先完成 Windows 真实环境验收，再发布正式版；
4. 所有范围变动先更新本计划和验收标准。

### 本轮不纳入范围

除非发现会阻断 `v0.5.0` 发布的问题，本轮暂不进行：

- 新格式化规则开发；
- 大规模 UI 视觉重设计；
- macOS 支持；
- 安装版或自动更新器；
- 大型编辑器功能，例如文件批处理、项目级规则集和云同步。

## 2. 已确认的关键决策

- 本轮目标版本为正式版 `v0.5.0`。
- 优先解决“全不选”与 Rust 空规则列表之间的语义问题。
- 在黄金样例回归测试体系建立前，不继续新增格式化规则或大型 UI 功能。
- 按本文档的阶段顺序推进开发。
- 本计划在任何代码改动之前同步到仓库 Markdown 文档。

## 3. 阶段总览

| 顺序 | 阶段 | 优先级 | 主要结果 |
| ---: | --- | --- | --- |
| 0 | 同步正式计划文档 | P0 | ✅ 已完成：计划落库并建立维护入口 |
| 1 | 修复“全不选”规则语义 | P0 | ✅ 已完成：规则选择状态不再有歧义 |
| 2 | 统一版本、路径与文档 | P0 | ✅ 已完成：测试、文档和实现一致 |
| 3 | 建立黄金样例回归测试 | P1 | ✅ 已完成：格式化行为具备数据驱动回归保护 |
| 4 | 提升设置文件可靠性 | P1 | ✅ 已完成：设置原子写入、备份与恢复 |
| 5 | 前端结构重构 | P1 | ✅ 已完成当前安全范围：拆分标题栏与设置弹窗；格式化 hook 延后 |
| 6 | 长文本性能与反馈 | P2 | ✅ 已完成：动态防抖、长文本状态和耗时提示 |
| 7 | 无障碍与键盘体验 | P2 | ✅ 已完成：键盘操作、焦点和动态状态完善 |
| 8 | CI 与发布流程加强 | P2 | ✅ 已完成：静态检查与发布保障增强 |
| 9 | Windows 验收与正式发布 | P0 | 完成真实环境验收并发布 `v0.5.0` |

## 后续 UI 与设置增强计划

本计划用于跟踪字号、主界面缩放、设置加载提醒和路径视觉样式改动；完成后应补充对应的 Rust/前端测试，并在正式发布前确认 Windows WebView2 与 Linux WebKitGTK 的布局表现。

- [x] 设置中增加编辑器字号选项，统一输入框与输出框字体、字号和行高；
- [x] 设置中增加主界面缩放选项，默认 100%，支持 80%、90%、100%、110%、125%；
- [x] 旧版本设置迁移、主设置损坏、备份损坏和恢复场景显示对应提醒；
- [x] 移除设置路径自定义悬停 tooltip 和下凹效果，改为灰色点状下划线；
- [x] 增加新设置字段、提醒状态、字体/缩放应用和路径样式的自动化测试；
- [x] 更新 README/docs/development.md 与发布计划说明。

## 4. 阶段 1：修复“全不选”与空规则列表语义 ✅

### 问题

前端“全不选”会生成空规则列表，而 Rust 当前管线把空规则列表解释为“启用全部规则”。必须消除这一双重语义。

### 目标语义

| 用户意图 | 后端语义 |
| --- | --- |
| 全选 | 执行全部已注册规则 |
| 恢复默认 | 执行所有默认启用规则 |
| 自定义选择 | 仅执行指定规则 |
| 全不选 | 不执行任何规则 |

### 实施内容

1. 已审核 `App.tsx`、`lib/tauri.ts`、Tauri command、`FormatRequest` 和 pipeline 的完整调用链；
2. 采用显式 `RuleSelection` 模型，避免依赖空数组表达多个含义；
3. 同步修改 Rust 请求模型、command、pipeline 及前端类型；
4. 保留设置文件中 `enabled: string[]` 的存储结构，其中空数组表示“全不选”；
5. 增加 Rust 和前端测试，覆盖全选、默认、自定义、全不选、未知 key 和恢复流程；
6. 在开发文档中记录最终规则选择 API 约定。

### 验收标准

- [x] 点击“全不选”后，输出与输入完全一致；
- [x] “恢复默认”仅启用注册表中的默认规则；
- [x] 空启用集重启恢复后仍表示“全不选”；
- [x] 不影响浏览器预览和 Tauri 正式运行模式；
- [x] Rust、前端测试与构建全部通过。

## 5. 阶段 2：统一版本、设置路径与文档表述 ✅

### 实施内容

1. 更新 `frontend/src/App.test.tsx` 中的旧版本 mock；
2. 将测试中的旧 `settings.json` 路径改为当前的 `rules.yaml` 路径；
3. 增加设置路径显示的精确断言；
4. 修订 `docs/development.md` 对历史 Python 设置逻辑与当前 Rust `rules.yaml` 的说明；
5. 核查 `package.json`、`Cargo.toml`、`tauri.conf.json`、版本脚本和 Release workflow 的版本策略；
6. 运行 `scripts/check_version.py` 验证版本一致性。

### 验收标准

- [x] 测试不再出现过期的 `0.4.x` 版本标记；
- [x] 测试不再把 `settings.json` 作为当前设置文件；
- [x] 文档准确区分旧 JSON 迁移和当前 YAML 设置；
- [x] 版本校验脚本通过。

## 6. 阶段 3：建立格式化引擎黄金样例回归测试 ✅

### 目标

在继续新增规则或大型 UI 功能前，将规则、保护层、换行和规则选择行为沉淀为数据驱动的回归样例。

### 建议目录

```text
src-tauri/tests/fixtures/
├── spacing.yaml
├── punctuation.yaml
├── markdown.yaml
├── latex.yaml
├── url-email.yaml
├── chemistry.yaml
├── newlines.yaml
├── rule-selection.yaml
└── regressions.yaml
```

### Fixture 最小结构

```yaml
name: 中文与英文之间插入空格
selection:
  mode: only
  keys:
    - spacing.cjk-latin
input: 在LeanCloud上
expected: 在 LeanCloud 上
```

### 覆盖范围

- 12 条现有规则；
- Markdown、LaTeX、URL、邮箱、代码块和化学式保护；
- `LF`、`CRLF`、`CR` 换行；
- emoji 和 CJK 扩展区；
- 全选、默认、指定规则、全不选；
- 规则组合、未知 key 和历史回归问题。

### 回归约定

- 每次修复格式化 bug，必须增加 `regressions.yaml` 案例；
- 每次新增规则，必须增加规则单测和组合案例；
- 每次修改保护层正则，必须补充保护边界案例；
- fixture 预期输出以当前产品规范为准，不机械依赖历史 Python 输出。

### 验收标准

- [x] Rust 测试自动加载并执行全部 fixture；
- [x] 每条规则和每类主要保护类型都有覆盖；
- [x] 四种规则选择模式都有覆盖；
- [x] 失败信息包含 fixture 文件和案例名；
- [x] 回归测试进入 CI 门禁。

## 7. 阶段 4：提升设置文件可靠性 ✅

### 实施内容

1. 将 `rules.yaml` 改为同目录临时文件写入、flush/sync 后原子 rename；
2. 增加 `rules.yaml.bak` 备份和恢复路径；
3. 主文件及备份均损坏时回退默认，并向前端报告状态；
4. 区分输入内容、规则开关、主题和字体的保存防抖策略；
5. 完善不可写目录和恢复状态的用户提示。

### 验收标准

- [x] 异常中断不会轻易产生半写入设置文件；
- [x] 主文件损坏时可以尝试从备份恢复；
- [x] UTF-8、多字节字符和旧 JSON 迁移继续通过；
- [x] 不可写目录错误可被用户理解。

## 8. 阶段 5：前端结构重构 ✅（当前安全范围）

### 建议拆分

```text
frontend/src/
├── hooks/
│   ├── useFormatter.ts
│   ├── useUserSettings.ts
│   ├── useThemeAndFont.ts
│   └── useWindowControls.ts
├── components/
│   ├── AppTitleBar.tsx
│   ├── FormatterEditor.tsx
│   ├── FormatterActions.tsx
│   ├── SettingsDialog.tsx
│   ├── RuleSettingsList.tsx
│   └── SaveStatus.tsx
└── App.tsx
```

### 实施约束

- `frontend/src/lib/tauri.ts` 仍是前端唯一 Tauri command 入口；
- 规则元数据仍由 Rust 注册表动态提供；
- 不复制规则表到前端；
- 不在本阶段进行视觉重设计；
- 保留异步序列号机制和现有交互行为。

### 验收标准

- [x] `App.tsx` 已主要负责页面组合、顶层状态和事件协调；
- [x] 标题栏和设置弹窗已拆分为独立组件；
- [x] 现有交互行为不变；
- [x] Vitest 和前端构建通过；
- [ ] 格式化 hook、主题 hook 和窗口控制 hook 延后到后续维护，避免在 `v0.5.0` 收尾阶段扩大异步状态重构风险。

## 9. 阶段 6：长文本性能与反馈 ✅

### 实施内容

- 按文本长度动态调整格式化防抖；
- 增加“正在排版”和长文本提示；
- 记录超过阈值的排版耗时；
- 建立 10 KB、100 KB、1 MB 文本的性能基线；
- 避免空输入和相同文本的重复格式化；
- 保留旧异步结果丢弃机制。

### 验收标准

- [x] 长文本编辑期间界面保持可响应；
- [x] 用户可以区分处理等待和程序无响应；
- [x] 新输入不会被旧结果覆盖；
- [x] 已建立动态防抖和前端长文本回归测试；完整 10 KB / 100 KB / 1 MB 性能基准延后到后续维护。

## 10. 阶段 7：无障碍与键盘体验 ✅

### 实施内容

- `Ctrl/Cmd + Enter`：立即格式化；
- `Ctrl/Cmd + Shift + C`：复制结果；
- `Ctrl/Cmd + ,`：打开设置；
- `Esc`：关闭设置；
- 完善弹窗焦点进入与关闭后的焦点恢复；
- 为规则分组、窗口按钮和动态状态补充可访问语义；
- 为复制、保存、错误和长文本状态增加 `aria-live` 提示。

### 验收标准

- [x] 不使用鼠标可以完成主要操作；
- [x] 弹窗关闭后焦点恢复到设置按钮；
- [x] 动态状态可被辅助技术感知；
- [x] `Ctrl/Cmd + Enter`、`Ctrl/Cmd + Shift + C` 和 `Ctrl/Cmd + ,` 已有前端测试；
- [x] 快捷键不影响普通文本输入；Radix Dialog 保留 `Esc` 关闭行为。

## 11. 阶段 8：CI 与发布流程加强 ✅

### 实施内容

在保留现有测试和构建流程的基础上增加：

```bash
cargo clippy --all-targets -- -D warnings
git diff --check
```

继续保持：

- Rust 格式检查和单元测试；
- 前端类型检查、Vitest 和 Vite build；
- Windows GUI 启动 smoke；
- Linux / Windows 打包；
- 版本一致性校验。

`cargo audit`、npm 审计、许可证清单和真实 Tauri E2E 测试作为后续评估项，除非发现会阻断发布的高危问题。

### 验收标准

- [x] CI 与本地推荐验证命令一致；
- [x] Clippy 无 warning；
- [x] Release 产物与版本号一致的既有流程保持不变；
- [x] 预发布 tag 不会错误占用 latest 的既有流程保持不变；
- [x] 正式 tag 能生成正式 Release 的既有流程保持不变。

## 12. Windows 验收与正式发布

### 当前预发布状态

- 已发布预发布 tag：`v0.5.0-pre1`、`v0.5.0-pre2`、`v0.5.0-pre3`、`v0.5.0-pre4`；Release 名称与 tag 保持一致，不添加 `CopyPolish` 前缀，均正确标记为 pre-release，不占用 latest；
- 当前 `dev` 基线已推进至规则调度、Span/Edit 迁移基础提交 `adab5ce`；后续后端重构继续在 `dev` 分支推进，并遵循“先测试、后迁移、不改变既有输出”的约束；
- 已完成本轮格式化边界修复并通过自动化验证：
  - Markdown 单星强调统计符号（`*t*`、`*p*`）与中文/比较运算符之间的空格边界；
  - `mg·mL⁻¹` 等 Unicode 上标结尾的科学单位与相邻中文之间的空格边界；
  - 对应黄金样例（`spacing.yaml`）、幂等回归与前端浏览器预览 fallback 同步；
- 已完成 CI 存储治理：本地 `src-tauri/target/` 缓存已清理（约 6.9 GiB）、远端全部 Actions artifacts 已删除、旧 `build` workflow 及冗余 runs 已清理，workflow artifact 保留期收紧为 1–3 天（详见 `docs/development.md` 持续集成章节）；
- 已发布 Release 上传 Windows `.exe` / `.7z` 及 Linux `.deb` / `.rpm` / `.AppImage` 资产，Release Notes 已整理；
- 真实 Windows 10/11 机器验收仍待完成，正式版 `v0.5.0` 暂不发布。

### Windows 人工验收

在真实 Windows 10/11 环境使用 `windows-portable` artifact 或候选 Release 产物验证：

#### 启动与窗口

- [ ] `CopyPolish.exe` 可直接启动；
- [ ] WebView2 正常工作；
- [ ] 无边框标题栏可拖动；
- [ ] 最小化、最大化和关闭按钮可用；
- [ ] 最小窗口尺寸为 `800 × 600`；
- [ ] 100%、125%、150%、200% DPI 下无关键布局问题。

#### 格式化

输入：

```text
在LeanCloud上，花了5000元
```

预期输出：

```text
在 LeanCloud 上，花了 5000 元
```

- [ ] 默认规则输出正确；
- [ ] 全选、恢复默认、单规则选择和全不选语义正确；
- [ ] 全不选时输入与输出完全一致；
- [ ] Markdown、URL、LaTeX、代码和化学式未被错误拆分。

#### 设置和错误处理

- [ ] 设置窗口显示 13 条规则；
- [ ] 规则修改后输出即时重排；
- [ ] 主题和字体立即生效；
- [ ] 设置路径显示 `rules.yaml`；
- [ ] 重启后恢复输入、规则、主题和字体；
- [ ] 不可写目录时显示清晰失败提示；
- [ ] 复制、清空和错误提示均正常。

### 发布前命令

在 Node 24 环境中执行：

```bash
npm ci --prefix frontend
npm test --prefix frontend -- --run
npm run build --prefix frontend

cargo fmt --manifest-path src-tauri/Cargo.toml --check
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
cargo test --manifest-path src-tauri/Cargo.toml

git diff --check
python3 scripts/check_version.py v0.5.0
```

### 正式发布步骤

1. 确认全部 P0 项完成；
2. 确认 Windows 人工验收通过；
3. 审查代码、测试结果、文档和本计划完成状态；
4. 合并或快进至 `master`；
5. 创建正式 tag `v0.5.0`；
6. 构建 Windows `.exe` / `.7z` 与 Linux `.deb` / `.rpm` / `.AppImage`（使用 GitLab 构建或按 [manual-release.md](release/manual-release.md) 本地构建，随后手动上传）；
7. 人工审查 Release Notes、版本号和全部资产；
8. 确认正式 Release 正确标记为 latest。

## 13. 发布门槛

以下事项完成前，不进入正式发布：
- [x] 阶段 0 计划文档已同步；
- [x] “全不选”语义修复完成并通过测试；
- [x] 版本、路径与文档表述已统一；
- [x] 黄金样例回归测试体系已建立并进入 CI；
- [x] 设置备份/恢复已实现并通过 Rust 测试；
- [x] 前端结构安全范围重构已完成；
- [x] 长文本提示与动态防抖已实现并通过前端测试；
- [x] 键盘快捷键、焦点恢复和动态无障碍状态已实现并通过前端测试；
- [x] Clippy、Rust 测试、前端测试、前端构建、版本校验和 `git diff --check` 已通过；
- [x] CI 存储治理已完成：本地 `target/` 缓存、远端 Actions artifacts 与旧 workflow runs 已清理，artifact 保留期收紧为 1–3 天；
- [x] 发布前自动化命令全部通过；
- [ ] Windows 真实环境验收通过；
- [x] `v0.5.0-pre2` Release 资产和 Release Notes 已人工复核；
- [ ] 正式版 `v0.5.0` Release 资产、Release Notes 和 latest 标记已复核。

## 14. 发布后移交

`v0.5.0` 正式发布完成后，复杂排版与 Unicode 基础能力增强（多行文本、特殊单位、数学符号、Markdown 结构、Unicode 边界——详见 [roadmap.md](roadmap.md) §5）、快捷键开关与自定义、本地构建自动化脚本、Unicode 引擎升级（unicode-segmentation / ICU4X）、真实 Tauri E2E、性能基准、前端 hooks 拆分以及依赖安全维护，均转入 [roadmap.md](roadmap.md) 跟踪；本地构建与手动上传发布的操作规范见 [manual-release.md](release/manual-release.md)。本计划文档在正式发布复核完成后归档，不再承载后续功能计划。
